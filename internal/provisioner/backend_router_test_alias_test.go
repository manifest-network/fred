package provisioner

// BackendRouter keeps legacy white-box fixtures concise without exporting the
// broad client-returning composition port from production builds.
type BackendRouter = backendRouter
